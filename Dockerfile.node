FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci 2>/dev/null || npm install

COPY server/ server/
COPY index.html .
COPY css/ css/
COPY js/ js/
COPY pages/ pages/
COPY assets/ assets/

ENV PORT=5000
EXPOSE 5000

CMD ["node", "server/server.js"]
